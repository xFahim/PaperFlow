<Page x:Class="PaperFlowWpf.Views.HomePageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PaperFlowWpf.Views"
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="800"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="HomePageView">

    <Page.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Trigger for Selected State -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="black" />
                                <!-- Custom Selection Color -->
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <!-- Trigger for MouseOver State -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A1D35" />
                                <!-- Hover Color -->
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>


    <Grid Margin="10">
        <!-- Header Section -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Welcome to PaperFlow" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,20" HorizontalAlignment="Center" />

        <!-- Main Content Section -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Explore Thesis Groups" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10" />


                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <ui:TextBox PlaceholderText="Search for a group" Width="200"></ui:TextBox>

                    <ComboBox  Margin="10,0,0,0" SelectedIndex="0">
                        <ComboBoxItem Content="Public Only" />
                        <ComboBoxItem Content="Private" />
                        <ComboBoxItem Content="All" />
                    </ComboBox>
                </StackPanel>

                <!-- Thesis Groups -->
                <UniformGrid Columns="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,10,0,0">
                    <!-- Example Thesis Cards -->
                    <Border Background="#2A1D35" CornerRadius="10" Padding="10" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Machine Learning in Agriculture" Foreground="White" FontSize="16" FontWeight="Bold" />
                            <TextBlock TextWrapping="Wrap" Text="Exploring the use of ML to enhance productivity" Foreground="Gray" FontSize="14" Margin="0,5,0,0" />
                            <Button Content="Start Working" Margin="0,10,0,0" Background="#5A3E85" Foreground="White" />
                        </StackPanel>
                    </Border>
                    <Border Background="#2A1D35" CornerRadius="10" Padding="10" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Blockchain for Supply Chains" Foreground="White" FontSize="16" FontWeight="Bold" />
                            <TextBlock TextWrapping="Wrap" Text="Implementing blockchain for transparency" Foreground="Gray" FontSize="14" Margin="0,5,0,0" />
                            <Button Content="Request Join" Margin="0,10,0,0" Background="#5A3E85" Foreground="White" />
                        </StackPanel>
                    </Border>

                    <Border Background="#2A1D35" CornerRadius="10" Padding="10" Margin="10">
                        <StackPanel>
                            <TextBlock Text="NLP bangla language" Foreground="White" FontSize="16" FontWeight="Bold" />
                            <TextBlock Text="work with nlp task like summary and all with bengali language" Foreground="Gray" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0" />
                            <Button Content="Request Join" Margin="0,10,0,0" Background="#5A3E85" Foreground="White" />
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>

            <!-- Right Section: My Groups and Progress -->
            <StackPanel Grid.Column="1" Background="#1E1E1E" Margin="10">
                <TextBlock Text="My Groups" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10" />
                <ListBox Background="#121212" Foreground="White" Height="300" SelectedIndex="0">
                    <!-- Group 1 -->
                    <ListBoxItem Padding="10">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon FontSize="20" Symbol="BrainCircuit24" Foreground="White"></ui:SymbolIcon>
                                <TextBlock Text="Group 1: AI in Medicine" FontSize="16" FontWeight="SemiBold" Margin="2 0 0 0" Foreground="White" />
                            </StackPanel>
                            <TextBlock Text="Members: 10" FontSize="14" Foreground="Gray" Margin="10,5,0,0" />
                            <TextBlock Text="Papers Read: 12 / 20" FontSize="14" Foreground="Gray" Margin="10,2,0,0" />
                            <ProgressBar Value="60" Height="10" Background="#2A2A2A" Foreground="#5A3E85" Margin="10,5,0,0" />
                            <Button Content="Start Working" Margin="10,10,0,0" Background="#5A3E85" Foreground="White" />
                        </StackPanel>
                    </ListBoxItem>

                    <!-- Group 2 -->
                    <ListBoxItem Padding="10">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon FontSize="20" Symbol="FoodGrains24" Foreground="White" Margin="0 0 4 0"></ui:SymbolIcon>
                                <TextBlock Text="Group 2: Renewable Energy Research" TextWrapping="Wrap" FontSize="16" FontWeight="SemiBold" Foreground="White" />
                            </StackPanel>
                            <TextBlock Text="Members: 8" FontSize="14" Foreground="Gray" Margin="10,5,0,0" />
                            <TextBlock Text="Papers Read: 15 / 25" FontSize="14" Foreground="Gray" Margin="10,2,0,0" />
                            <ProgressBar Value="70" Height="10" Background="#2A2A2A" Foreground="#5A3E85" Margin="10,5,0,0" />
                            <Button Content="Visit My Group" Margin="10,10,0,0" Background="#5A3E85" Foreground="White" />
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>

                <TextBlock Text="My Progress" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="20,20,0,10" />
                <ProgressBar Value="70" Height="20" Background="#2A2A2A" Foreground="#5A3E85" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
